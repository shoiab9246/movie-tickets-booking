import React, { Component } from 'react';
import { Link, Router, Routes, withRouter, Route } from 'react-router-dom';
import * as ROUTES from '../../Constants/routes.js';
import NavBar from '../navbar';
import { InputGroup, InputGroupText, InputGroupAddon, FormInput, Button, ButtonGroup, FormCheckbox } from "shards-react";

const userdashboard = () => {

    return (  

      <div style={{ textAlign: 'center', backgroundColor:'thistle', height:'150vh'}}>
     
      
      </div>
      
    );
  }

  export default userdashboard;